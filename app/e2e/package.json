{
  "name": "@hcm/e2e",
  "private": true,
  "type": "module",
  "description": "HCM Core-HR slice — Playwright end-to-end + accessibility evidence. A scripted keyboard walkthrough of the ARIA-tree org chart and an axe (WCAG 2.2 AA) pass over the directory, org chart, and person views, driven against the real API + real Neo4j graph.",
  "scripts": {
    "db:up": "docker compose -f ../docker-compose.yml up -d --wait neo4j",
    "db:seed": "npm run seed -w @hcm/server",
    "db:down": "docker compose -f ../docker-compose.yml down",
    "install:browsers": "playwright install --with-deps chromium",
    "pretest": "npm run db:up && npm run db:seed",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.20.1",
    "typescript": "^5.7.3"
  }
}
